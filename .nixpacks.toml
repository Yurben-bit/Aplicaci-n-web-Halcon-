[phases.setup]
nixPkgs = [
  "php82Extensions.bcmath",
  "php82Extensions.mbstring",
  "php82Extensions.pdo_mysql",
  "nodejs_18",
  "npm-9_x"
]

[phases.build]
cmds = [
  "npm install",
  "npm run build"
]
